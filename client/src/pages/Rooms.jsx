function Rooms({ isDarkMode }) {
  const textColor = isDarkMode ? "text-gray-800" : "text-gray-100";
  return (
    <h1 className={`text-center text-xl text-[#ffffff] mx-auto my-auto ${textColor}`}>
      Rooms Work under Progress.......
    </h1>
  );
}
export default Rooms;
